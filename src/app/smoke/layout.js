export const metadata = {
  title: 'SansCigarette — Arrêter de fumer, c\'est possible',
  description: 'Application gratuite pour arrêter de fumer. Suivi en temps réel, étapes santé, économies calculées, gestion des envies.',
};

export default function SmokeLayout({ children }) {
  return children;
}
